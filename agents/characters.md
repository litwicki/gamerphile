# Characters page

On the `/characters` page, display a grid of all characters associated to the active users profile. We do this by querying the WoW API for their wow profile and then fetching all associated characters.

Add separate filters on the page that allow the user to filter by level, class, realm, and guild. Users should also be able to sort by level, class name, or realm.